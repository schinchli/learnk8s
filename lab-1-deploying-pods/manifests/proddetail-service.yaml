apiVersion: v1
kind: Service
metadata:
  name: proddetail
  namespace: workshop
  labels:
    app: proddetail
  annotations:
    owner: student
spec:
  type: ClusterIP
  ports:
    - port: 3000
      name: http
  selector:
    app: proddetail
